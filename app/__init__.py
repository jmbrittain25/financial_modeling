"""
Financial Simulator — App / UI layer.

This package contains the Streamlit-based interactive scenario builder
and supporting components (distribution picker, templates, etc.).

It is intentionally kept outside the `src/financial_simulator` core
so that the core library remains lightweight and has no Streamlit
or UI dependencies.
"""
